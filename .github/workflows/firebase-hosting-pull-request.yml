# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on PR
on: pull_request
permissions:
  checks: write
  contents: read
  pull-requests: write
jobs:
  build_and_preview:
    if: ${{ github.event.pull_request.head.repo.full_name == github.repository }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: cd reflections-react-code && npm ci && npm run build
        env:
          REACT_APP_FIREBASE_API_KEY: AIzaSyDDZNggvPgspFMo_r8hdq_rkT2Ep-f45PE
          REACT_APP_FIREBASE_AUTH_DOMAIN: a-reflections.firebaseapp.com
          REACT_APP_FIREBASE_PROJECT_ID: a-reflections
          REACT_APP_FIREBASE_APP_ID: 1:901114266665:web:7934ad0726ca7cc02c0838
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_A_REFLECTIONS }}
          projectId: a-reflections
