# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: cd reflections-react-code && npm ci && npm run build  && npm install -g firebase-tools &&
          firebase deploy --only hosting --project a-reflections
        env:
          FIREBASE_AUTH: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_A_REFLECTIONS }}
